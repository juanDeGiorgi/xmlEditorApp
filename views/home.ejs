<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- boostrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <!-- boostrap -->

    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <title>Home</title>
  </head>
  <body class="home">
    <%- include('./partials/header') %>
    <main class="main-home">
      <%if(!locals.nodos || nodos.length === 0 ){%>
      <div class="alert alert-danger" role="alert">
        <h4>Error no se a podido establecer la conexión con el Bucket</h4>
      </div>
      <%}else {%>
      <h1 class="container-nodos-title">Nodos :</h1>

      <div class="container-nodos">
        <%locals.nodos.forEach((nodo,index) => {%>
        <div class="card card-nodo">
          <div class="mb-3">
            <label for="name" class="form-label">Nombre</label>
            <input
              type="text"
              class="form-control"
              id="Name"
              name="Name"
              disabled
              value="<%=nodo.Name%>"
            />
          </div>

          <div class="mb-3">
            <label for="OBJname" class="form-label">Archivo</label>
            <input
              type="text"
              class="form-control"
              id="OBJName"
              name="OBJName"
              disabled
              value="<%=nodo.OBJName%>"
            />
          </div>

          <div class="mb-3">
            <label for="scale" class="form-label">Escala</label>
            <input
              type="text"
              class="form-control"
              id="Scale"
              name="Scale"
              disabled
              value="<%=nodo.Scale%>"
            />
          </div>

          <div class="container-card-butons">
            <a href="/nodes/edit/<%=index%>" class="btn btn-primary options-buttons"
              >Editar</a
            >

            <form id="deleteForm<%=nodo.id%>" action="/nodes/delete/<%=nodo.id%>?_method=DELETE" method="POST">
              <button type="submit" class="btn btn-danger options-buttons">Eliminar</button>
            </form>
          </div>

        </div>
        <%})%>
      </div>
      <%}%>
    </main>
    <footer></footer>
    <!-- boostrap -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
    <!-- boostrap -->

    <!-- sweetAlert -->
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- sweetAlert -->

    <script src="/javascripts/index.js"></script>
    <script src="/javascripts/home.js"></script>
  </body>
</html>
